package com.saga.be.entity.enums;

/** Admin anomaly signal identifiers. Unsupported types stay TBD until product authority exists. */
public enum AdminAnomalySignalType {
    OVERDUE_TASK,
    MSR,
    DEADLINE_PROCESS,
    SNA_ISOLATION
}
